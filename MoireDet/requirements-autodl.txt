# Install PyTorch separately so the CUDA wheel source is explicit:
# python -m pip install torch==2.8.0 torchvision==0.23.0 \
#   --index-url https://download.pytorch.org/whl/cu126

# Clean AutoDL runtime selected for MoireDet inference and the current training path.
# NumPy 1.26 avoids the NumPy-2 ABI conflicts found in the local mixed environment.
numpy==1.26.4
opencv-python-headless==4.11.0.86
einops==0.8.1
Pillow==11.0.0
tqdm==4.67.1
colorlog==6.9.0
matplotlib==3.10.3
